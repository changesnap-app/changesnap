name: keepalive
on: {schedule: [{cron: '*/10 * * * *'}], workflow_dispatch: {}}
jobs: {ping: {runs-on: ubuntu-latest, timeout-minutes: 3, steps: [{run: "curl -s -m 90 -o /dev/null -w '%{http_code}' https://changesnap.onrender.com/api/health || true"}]}}
